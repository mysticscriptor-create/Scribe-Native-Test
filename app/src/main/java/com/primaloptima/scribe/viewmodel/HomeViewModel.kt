package com.primaloptima.scribe.viewmodel

/*
 * HomeViewModel was split into HomeShellViewModel, DashboardViewModel,
 * BooksViewModel, NotesViewModel, and StatsViewModel.
 *
 * This intentionally empty compatibility file is kept so the edited-files ZIP
 * can replace the former source path when extracted over the original project.
 */